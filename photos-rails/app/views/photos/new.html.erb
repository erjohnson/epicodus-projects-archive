<h1>Upload a photo!</h1>

<%= form_for @photo, :url => photos_path, :html => { :multipart => true } do |form| %>
  <%= form.file_field :picture %>
  <%= form.hidden_field :uploaded_by, :value => current_user.id %>
  <%= form.submit "Create" %>
<% end %>
